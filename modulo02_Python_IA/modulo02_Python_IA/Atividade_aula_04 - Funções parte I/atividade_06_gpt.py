# ✅ 5. Verificar maior valor
# Enunciado:
# Crie uma função maior_valor(lista) que recebe uma lista de números e retorna o maior número da lista.

lista = [1, 2, 50, 4, 5]
def maior_valor(lista):
    maior = max(lista)
    return maior
print(maior_valor(lista))